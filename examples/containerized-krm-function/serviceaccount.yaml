apiVersion: v1
kind: ServiceAccount
metadata:
  name: workload-identity-sa
  namespace: default
  annotations:
    azure.workload.identity/client-id: '{{ tfstate `azurerm_user_assigned_identity.workload.client_id` }}'
